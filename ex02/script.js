    let valor = Number(prompt("Digite o valor do seu pedido:"));
    let desconto = valor * 0.10;
    let total = valor - desconto;
    if (valor > +50);
    alert("Você possue o cartão fidelidade?")
    let opcao = prompt ("sim/não");
    if (opcao == "sim") {
        alert("Parabéns! você ganhou um 10% de desconto!!")
    }
    if (opcao == "não")
        alert("Ah! eu sinto muito mais você não ganhou um desconto...Toma um Cookie para se sentir melhor")